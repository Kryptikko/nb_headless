// import type { Vec4 } from "./math"

interface Theme {
  background: string
  text: string
  // background: string | Vec4
  // text: string | Vec4
}
const theme: Theme = {
  background: "#00000",
  text: "#FFFFFF"
}

export default theme
